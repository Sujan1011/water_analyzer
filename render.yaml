services:
  - type: web
    name: water-test-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
    autoDeploy: true
